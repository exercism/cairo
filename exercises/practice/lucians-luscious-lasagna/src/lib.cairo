pub fn expected_minutes_in_oven() -> u32 {
    panic!("implement `expected_minutes_in_oven`: returns expected minutes in the oven")
}

pub fn remaining_minutes_in_oven(actual_minutes_in_oven: u32) -> u32 {
    panic!(
        "implement `remaining_minutes_in_oven`: calculates remaining minutes in oven given actual minutes in oven: {actual_minutes_in_oven}"
    )
}

pub fn preparation_time_in_minutes(number_of_layers: u32) -> u32 {
    panic!("implement `preparation_time_in_minutes`: calculates preparation time in minutes for number of layers: {number_of_layers}")
}

pub fn elapsed_time_in_minutes(number_of_layers: u32, actual_minutes_in_oven: u32) -> u32 {
    panic!(
        "implement `elapsed_time_in_minutes`: calculates elapsed time in minutes for number of layers {number_of_layers} and actual minutes in oven {actual_minutes_in_oven}"
    )
}
