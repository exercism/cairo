pub fn find(search_array: @Array<usize>, value: usize) -> Option<usize> {
    panic!(
        "implement `find`: uses the binary search algorithm to find the element '{value}' in the array '{search_array:?}' and returns its index."
    )
}
